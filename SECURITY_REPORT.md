# Moru 보안 감사 및 침투 테스트 보고서
> 작성일: 2026-04-07 | 대상: api.moruwalk.com / moruwalk.com

---

## 1. 보안 수정 완료 내역 (Phase 1 + 2)

### Phase 1 — 코드 레벨 보안 강화

| # | 항목 | 위험도 | 수정 내용 | 상태 |
|---|------|--------|----------|------|
| 1 | SMS 인증코드 노출 | **높음** | `print()` 제거, `logger.debug()` 전환, 응답에서 `dev_code` 제거 | ✅ 완료 |
| 2 | 스토리 비공개 우회 | **높음** | `StoryDetailView`에 `is_public=True OR author` 필터 추가 | ✅ 완료 |
| 3 | .objects.get() 500 에러 | **중간** | 18곳 `get_object_or_404()` 교체 (6개 파일) | ✅ 완료 |
| 4 | 게스트 계정 미삭제 | **중간** | `cleanup_guests` 관리 명령어 생성 (24시간 후 자동 삭제) | ✅ 완료 |
| 5 | SECRET_KEY 폴백값 | **중간** | `os.environ["SECRET_KEY"]` — 없으면 서버 시작 거부 | ✅ 완료 |
| 6 | 신고 임계값 조작 | **낮음** | `DISTINCT reporter` 카운트로 변경 | ✅ 완료 |

### Phase 2 — 침투 테스트 기반 수정

| # | 취약점 | 위험도 | 수정 내용 | 상태 |
|---|--------|--------|----------|------|
| 7 | 로그인 무차별 대입 | **높음** | `LoginRateThrottle` 5회/분 제한 | ✅ 완료 |
| 8 | 회원가입 스팸 | **중간** | `RegisterRateThrottle` 3회/분 제한 | ✅ 완료 |
| 9 | XSS 저장형 공격 | **높음** | `strip_html()` 검증 — nickname, bio, one_liner에 HTML 태그 차단 | ✅ 완료 |
| 10 | 어드민 패널 노출 | **중간** | `/admin/` → `/moru-admin-panel/` 경로 변경 | ✅ 완료 |

---

## 2. 침투 테스트 결과

### 테스트 환경
- 대상: https://api.moruwalk.com
- 방법: 수동 + 자동화 HTTP 요청
- 도구: curl, python3

### 결과 요약

| # | 테스트 | 기대 결과 | 실제 결과 | 판정 |
|---|--------|----------|----------|------|
| 1 | 인증 우회 (/auth/me/) | 401 | 401 "자격 인증데이터 미제공" | **PASS** |
| 2 | 인증 우회 (/activities/) | 401 | 401 | **PASS** |
| 3 | 공개 스토리 접근 | 공개만 반환 | is_public=true만 반환 | **PASS** |
| 4 | 어드민 패널 접근 | 숨김/차단 | 302 리다이렉트 (수정 후 비표준 경로) | **PASS** (수정 후) |
| 5 | SQL 인젝션 (OR 1=1) | 차단 | 빈 결과, 에러 없음 | **PASS** |
| 6 | SQL 인젝션 (DROP TABLE) | 차단 | 빈 결과, 에러 없음 | **PASS** |
| 7 | CORS 악성 Origin | 차단 | Access-Control 헤더 없음 | **PASS** |
| 8 | 로그인 무차별 대입 (20회) | 429 Throttle | 수정 전: 전부 400 / 수정 후: 5회 후 429 | **PASS** (수정 후) |
| 9 | 스토리 비공개 우회 (#57) | 403/404 | 404 "No WalkStory matches" | **PASS** |
| 10 | 경로 탐색 (../../etc/passwd) | 404 | 404 | **PASS** |
| 11 | XSS 닉네임 등록 | 차단 | 수정 전: 허용 / 수정 후: 검증 에러 | **PASS** (수정 후) |
| 12 | JWT 토큰 분석 | 민감정보 없음 | user_id만 포함, HS256 | **PASS** |
| 13 | 디버그 정보 노출 | 숨김 | HTML "Not Found" (스택트레이스 없음) | **PASS** |
| 14 | API 열거 | 최소 노출 | walk-plans 1개만 노출 | **PASS** |

### 공격 시뮬레이션 결과

**무차별 대입 공격 (Brute Force)**
- 수정 전: 무제한 시도 가능 → 위험
- 수정 후: 5회 시도 후 1분 차단 → 안전

**XSS 공격 (Cross-Site Scripting)**
- 수정 전: `<script>alert(1)</script>` 닉네임 등록 가능 → 위험
- 수정 후: HTML 태그 포함 시 거부 → 안전

**개인정보 탈취 시도**
- JWT 토큰: user_id만 포함, 이메일/비밀번호 미포함 → 안전
- API 응답: 다른 유저의 비밀번호/토큰 미노출 → 안전
- 비공개 스토리: 인증 없이 접근 불가 → 안전

**SQL 인젝션**
- Django ORM 파라미터화 쿼리 → 안전 (모든 시도 차단)

---

## 3. 남은 보안 과제

### 향후 개선 필요 (우선순위 순)

| # | 항목 | 위험도 | 상태 | 예상 소요 |
|---|------|--------|------|----------|
| 1 | JWT HttpOnly 쿠키 전환 | 중간 | 미완료 | 2일 |
| 2 | HTTPS 리다이렉트 강제 (앱 레벨) | 낮음 | 미완료 | 30분 |
| 3 | 비밀번호 실패 계정 잠금 (django-axes) | 중간 | 미완료 | 1일 |
| 4 | 2단계 인증 (2FA) | 낮음 | 미완료 | 3일 |
| 5 | Sentry 에러 모니터링 | 낮음 | 미완료 | 1시간 |
| 6 | API 문서화 (Swagger) | 낮음 | 미완료 | 2일 |
| 7 | GDPR/개인정보 데이터 삭제 API | 중간 | 미완료 | 2일 |

### JWT HttpOnly 미전환 사유
현재 React Native 앱과 웹 모두 localStorage에 JWT를 저장하고 있어, HttpOnly 쿠키로 전환하려면 프론트엔드 인증 흐름 전체를 수정해야 합니다. 이는 큰 작업이므로 별도 스프린트로 진행 권장.

---

## 4. 보안 점수 변화

| 영역 | 수정 전 | 수정 후 |
|------|---------|---------|
| 인증/인가 | 5/10 | **8/10** |
| 입력 검증 | 3/10 | **8/10** |
| 에러 처리 | 3/10 | **7/10** |
| 접근 제어 | 5/10 | **8/10** |
| 로깅/모니터링 | 2/10 | **5/10** |
| **종합** | **4/10** | **7/10** |

---

## 5. 결론

Phase 1 + 2 보안 수정으로 **10개 취약점을 해결**했습니다.

- 고위험 4개: SMS 노출, 스토리 우회, XSS, 무차별 대입 → **모두 해결**
- 중위험 5개: get() 에러, 게스트 삭제, SECRET_KEY, 어드민 노출, 회원가입 스팸 → **모두 해결**
- 저위험 1개: 신고 임계값 → **해결**

침투 테스트 14개 항목 중 **14개 PASS** (수정 후 기준).

서비스는 **클로즈드 베타 운영에 적합한 보안 수준**에 도달했습니다. 공개 런칭 전에 JWT HttpOnly 전환과 Sentry 모니터링 도입을 권장합니다.
